---
phase: 13-about-branding-help
plan: 03
type: execute
wave: 3
depends_on: [13-02]
files_modified: [src/ui/assets/icons/, build script or PyInstaller spec]
autonomous: true

must_haves:
  truths:
    - "A multi-size .ico file exists for Windows (e.g. in src/ui/assets/icons/ or package/)"
    - "Build process (PyInstaller/Nuitka/spec) is configured to use that .ico as executable icon"
    - "No refactor of build tooling logic — only icon path / config change"
  artifacts:
    - path: "src/ui/assets/icons/app.ico"
      provides: "Multi-size ICO for Windows Explorer and taskbar"
  key_links:
    - from: "PyInstaller spec or build script"
      to: "src/ui/assets/icons/app.ico"
      via: "icon= argument or equivalent"
---

<objective>
Ensure the Windows build shows the custom app icon in Explorer and taskbar. Add or designate a multi-size .ico; configure the build (e.g. PyInstaller .spec or build_windows.py) to use it. No change to build logic beyond icon configuration. See 13-DISCUSS.md §4.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-about-branding-help/13-DISCUSS.md
@.planning/phases/13-about-branding-help/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Add or generate app.ico for Windows</name>
  <files>src/ui/assets/icons/app.ico</files>
  <action>
Provide a multi-size .ico file (e.g. 16x16, 32x32, 48x48, 256x256) for Windows. Place it at src/ui/assets/icons/app.ico (or an agreed path referenced by the build). If the project already has an SVG/PNG app icon from 13-02, create .ico from it (e.g. with ImageMagick, Pillow, or an online tool). Otherwise add a minimal placeholder .ico so the build step can be wired; it can be replaced later with final artwork.
  </action>
  <verify>File app.ico exists and is a valid ICO (optional: python -c "from PIL import Image; Image.open('src/ui/assets/icons/app.ico')" or similar).</verify>
</task>

<task type="auto">
  <name>Configure build to use custom icon</name>
  <files>EPG_PDF_Extraherare.spec or build_windows.py or equivalent</files>
  <action>
In the PyInstaller spec file (or build script that calls PyInstaller), set the icon for the executable to the new .ico path, e.g. icon="src/ui/assets/icons/app.ico" in the EXE() or Analysis() call. Do not change other build logic (entry point, data files, etc.) — only add or update the icon parameter. If the build is invoked from a script, ensure the path is relative to the project root or resolved correctly.
  </action>
  <verify>Run the Windows build; built .exe shows custom icon in Explorer and in taskbar when run.</verify>
</task>

</tasks>

<success_criteria>
- Multi-size .ico exists; build uses it for the executable. Windows .exe displays correct icon. Changes limited to icon asset and build icon config.
</success_criteria>
