---
phase: 13-about-branding-help
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/ui/views/main_window.py, src/ui/views/about_dialog.py]
autonomous: true

must_haves:
  truths:
    - "MainWindow has a Help menu with an About action that opens AboutDialog"
    - "AboutDialog is a QDialog with two tabs: 'Om appen' and 'Hjälp'"
    - "Tab 'Om appen' shows app name, version (get_app_version()), description (PDF → analysis → review → export), and 'Skapad av: <author>'"
    - "Tab 'Hjälp' shows step-by-step usage (1–5) and short troubleshooting"
  artifacts:
    - path: "src/ui/views/about_dialog.py"
      provides: "AboutDialog: QTabWidget with Om appen + Hjälp, theme styling"
  key_links:
    - from: "src/ui/views/main_window.py"
      to: "src/ui/views/about_dialog.py"
      via: "Help → About opens AboutDialog"
---

<objective>
Add a professional About dialog and Help menu. Dialog has tabs: (a) "Om appen" — app name, version, short description, "Skapad av: &lt;author&gt;"; (b) "Hjälp" — step-by-step usage (Open PDF, Run, Review, Select candidate, Export) + short troubleshooting. Menu: Help → About. See 13-DISCUSS.md §1.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-about-branding-help/13-DISCUSS.md
@.planning/phases/13-about-branding-help/13-CONTEXT.md
@src/ui/views/main_window.py
@src/config.py
</context>

<tasks>

<task type="auto">
  <name>Create AboutDialog with tabs in about_dialog.py</name>
  <files>src/ui/views/about_dialog.py</files>
  <action>
Create src/ui/views/about_dialog.py with class AboutDialog(QDialog).

**Structure:**
- Window title: "Om EPG PDF Extraherare".
- QTabWidget with two tabs:
  - **"Om appen"**: QWidget with QVBoxLayout containing:
    - App name (e.g. "EPG PDF Extraherare"), large/emphasized.
    - Version from get_app_version() (import from ...config or src.config).
    - Short description: that the app does PDF → analysis → review → export (one or two sentences).
    - "Skapad av: &lt;author&gt;" — use a placeholder author name or read from pyproject.toml / constant; e.g. "Skapad av: EPG" or configurable string.
  - **"Hjälp"**: QWidget with QVBoxLayout containing:
    - Step-by-step usage as plain text or QLabels: 1) Öppna PDF, 2) Kör analys, 3) Granska resultat/varningar, 4) Välj kandidat vid behov, 5) Exportera till Excel.
    - Short troubleshooting: saknade resultat, fel, låg konfidens / AI-användning (2–3 punkter).
- Bottom: "Stäng" or "OK" button calling accept().
- setObjectName("aboutDialog") for theme; use existing theme classes if defined (e.g. muted for secondary text).
  </action>
  <verify>python -c "from src.ui.views.about_dialog import AboutDialog; from PySide6.QtWidgets import QApplication; import sys; app=QApplication(sys.argv); d=AboutDialog(); d.show(); print('AboutDialog ok')"</verify>
  <done>AboutDialog opens with tabs "Om appen" and "Hjälp"; shows app name, version, Skapad av, steps and troubleshooting.</done>
</task>

<task type="auto">
  <name>Add Help menu and About action in MainWindow</name>
  <files>src/ui/views/main_window.py</files>
  <action>
In setup_menu_bar (or equivalent), add a "Hjälp" (Help) menu. Add action "Om EPG PDF Extraherare..." (or "Om...") that opens AboutDialog.

- Connect to a slot: instantiate AboutDialog(parent=self), call .exec() for modal.
- Import AboutDialog from .about_dialog. Optional: add toolbar button or shortcut (e.g. F1) for About if it fits without clutter.
  </action>
  <verify>Run run_gui.py; open Hjälp → Om; confirm tabbed About dialog shows Om appen (name, version, Skapad av) and Hjälp (steps + troubleshooting).</verify>
  <done>Help menu exists; About opens modal dialog with both tabs; user can read help and credit.</done>
</task>

</tasks>

<success_criteria>
- Help menu with About; AboutDialog has tabs "Om appen" and "Hjälp" with content and author credit. Changes under src/ui/.
</success_criteria>
